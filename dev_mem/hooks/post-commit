#!/usr/bin/env bash
# dev-mem git post-commit hook
# Installed by: dev-mem init
#
# Records the latest commit to the dev-mem database.
# Runs in the background so it never blocks the git workflow.
dev-mem collect git-commit &
